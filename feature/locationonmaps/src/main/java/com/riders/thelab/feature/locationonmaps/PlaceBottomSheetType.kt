package com.riders.thelab.feature.locationonmaps

enum class PlaceBottomSheetType {
    // when user search a place and get the result for the exact location
    SEARCH_LOCATION,

    // Can be anything clicked on the map
    POI;
}